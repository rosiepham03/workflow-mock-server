applications:
- name: workflow-mock-api
  memory: 512M
  buildpacks:
    - java_buildpack
  command: java -Dserver.port=$PORT -jar target/workflow-mock-server-1.0.0.jar
  health-check-type: http
  health-check-http-endpoint: /
  default-route: true
  env:
    PORT: 8082